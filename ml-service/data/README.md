# Datasets

Raw datasets are intentionally excluded from Git due to GitHub's file size limits (e.g., Flood training dataset >50MB).
Development datasets (from Kaggle or similar) are strictly for local model tuning and mock inference. They do not represent live production data.

If you need to retrain, download the datasets directly into the `raw/` directory according to your configured paths.
