# Models

Large trained ML models (`*.pkl`, `*.joblib`) are intentionally excluded from Git due to file size limits.
If you clone this repository, you must either train the models locally using the provided training scripts or fetch the pre-trained weights from a dedicated ML storage bucket.
